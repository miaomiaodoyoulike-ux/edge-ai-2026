# Week 01 Day 01 Notes

## Completed

- Created the learning repository structure.
- Finished three Python scripts:
  - `01_grade_average.py`
  - `02_word_count.py`
  - `03_file_renamer.py`
- Finished the C memory program:
  - `array_sum.c`
- Observed that adjacent `int` array elements differ by 4 bytes in memory.
- Finished Day 01 LeetCode notes:
  - Two Sum
  - Valid Parentheses

## Key Takeaways

- Python `list` can store a group of values.
- Python `dict` can store key-value pairs, such as `word -> count`.
- `for` loops can process items one by one.
- C arrays are stored continuously in memory.
- Git Bash commands needs correct paths, such as `/d/EdgeAIProjects/...`.

## Problems

- Need more practice with Python dictionary syntax.
- Need more practice with Git Bash path navigation.
- `gcc` is not available in Git Bash yet, so C code was run with Dev-C++.

## Tomorrow

- Practice Python file operations.
- Continue using Git Bash for command-line practice.
- Improve README and commit workflow.